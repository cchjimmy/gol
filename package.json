{
	"name": "gol",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"build": "esbuild ./src/main.ts --bundle --minify --outfile=build/out.js",
		"serve": "esbuild --servedir=.",
		"start": "npm run build && npm run serve"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"devDependencies": {
		"esbuild": "^0.20.2"
	}
}
